# RAG Benchmarking Configuration

# Your RAG system API URL
API_BASE_URL=http://localhost:8000

# OpenAI API key (required for RAGAS evaluation)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Optional: Cost per 1000 tokens for your LLM
# Adjust based on your model pricing
# Examples:
# - GPT-4o-mini: 0.00015 (input) + 0.0006 (output) ≈ 0.0004 average
# - GPT-3.5-turbo: 0.0005 (input) + 0.0015 (output) ≈ 0.001 average
# - Claude Haiku: 0.00025 (input) + 0.00125 (output) ≈ 0.0007 average
COST_PER_1K_TOKENS=0.0015

# Optional: Anthropic API key (if using Claude for evaluation)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Google API key (if using Gemini for evaluation)
GOOGLE_API_KEY=...
